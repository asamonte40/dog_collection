<style>
body {
  font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
  background-color: #fef6f5;
  color: #333;
  line-height: 1.6;
  padding: 1rem;
}
</style>

<h1><%= @dog.name %></h1>

<p><strong>Breed:</strong>
  <%= link_to @dog.breed.name, breed_path(@dog.breed) %>
</p>

<p><strong>Owner:</strong>
  <%= link_to "#{@dog.owner.first_name} #{@dog.owner.last_name}", owner_path(@dog.owner) %>
</p>

<p><strong>Description:</strong> <%= @dog.breed.description %></p>

<%= image_tag @dog.image_url, class: "img-fluid mt-3 rounded" if @dog.image_url.present? %>
